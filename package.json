{
  "name": "mrs-solar",
  "version": "1.0.0",
  "description": "MRS SOLAR - Solar Panel Loan Management System",
  "private": true,
  "scripts": {
    "build": "npm install && npx vite build || (cd frontend && npm install && npm run build) && (cd backend && npm install || true)",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
